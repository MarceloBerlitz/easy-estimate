version: 2.1

executors:
  my-executor:
    docker:
      - image: node:18-alpine
    working_directory: /tmp

jobs:
  build:
    executor: my-executor
    steps:
      - checkout
      - run:
          command: npm i -g yarn
          name: Install yarn
      - run:
          command: yarn test
          name: Run tests
      - run:
          command: yarn build
          name: Build app
      - persist_to_workspace:
          root: easy-estimate
          paths: .
  deploy:
    executor: my-executor
    steps:
      - attach_workspace:
        at: /tmp/easy-estimate
      - run:
          name: Deploy app
          command: |
            scp -r ./packages/server/dist/ ${DEPLOY_USER}@${DEPLOY_HOST}:${DEPLOY_DIR}/.
