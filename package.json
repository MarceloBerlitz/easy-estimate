{
  "name": "easy-estimate",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "yarn workspace @ee/server start & yarn workspace @ee/web start",
    "build": "yarn workspaces run build",
    "build:lib": "yarn workspace @ee/lib build"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
